{
  "name": "git-avance-course",
  "version": "1.0.0",
  "description": "Formation Git avancé : internals, branching strategies, rebase interactif, bisect, hooks, worktrees, submodules, monorepos, workflows (intermédiaire → expert)",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
